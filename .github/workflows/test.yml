name: Test application

on:
  pull_request:
    branches: [ main, develop ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      APP_SECRET_KEY: secret-key

    steps:
      - uses: actions/checkout@v2

      - name: Docker - build image
        run: docker build -t msaqusers .

      - name: Docker - run container
        run: docker run --name msaqusers -e APP_SECRET_KEY=$APP_SECRET_KEY -e FLASK_ENV=testing -d msaqusers

      - name: Docker - logs
        run: docker logs msaqusers

      - name: Docker - stop container
        run: docker stop msaqusers

      - name: Docker - remove container
        run: docker rm msaqusers
